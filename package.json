{
  "scripts": {
    "start": "cd ./infrastructure/apigateway/ && sls deploy -s local-no-mount && cd ../.. && sls deploy -s local-no-mount"
  },
  "devDependencies": {
    "serverless-localstack": "^1.2.0",
    "serverless-openapi-documentation": "^0.4.0",
    "serverless-python-requirements": "^6.0.1"
  }
}
